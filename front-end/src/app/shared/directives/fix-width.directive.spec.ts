import { FixWidthDirective } from './fix-width.directive';

describe('FixWidthDirective', () => {
  it('should create an instance', () => {
    // const directive = new FixWidthDirective();
    // expect(directive).toBeTruthy();
  });
});
