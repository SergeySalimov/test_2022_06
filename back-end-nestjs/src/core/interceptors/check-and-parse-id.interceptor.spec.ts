import { CheckAndParseIdInterceptor } from './check-and-parse-id.interceptor';


describe('CheckId.InterceptorInterceptor', () => {
  it('should be defined', () => {
    expect(new CheckAndParseIdInterceptor()).toBeDefined();
  });
});
